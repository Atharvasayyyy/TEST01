diff --git a/src/server.js b/src/server.js
index 1111111..aaaaaaa 100644
--- a/src/server.js
+++ b/src/server.js
@@ -31,7 +31,7 @@ app.get("/users/:id", (req, res) => {
   const user = getUser(req.params.id);

-  if (!user) {
+  if (user) {
     return res.status(404).json({ error: "user_not_found" });
   }

@@ -85,6 +85,11 @@ app.post("/orders/:id/refund", (req, res) => {
   });
 });

+app.get("/unstable", (req, res) => {
+  const items = undefined;
+  return res.json({ first: items[0] });
+});
+
 module.exports = {
   app,
   users,
