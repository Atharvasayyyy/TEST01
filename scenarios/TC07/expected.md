Expected concern: the test is vacuous/weak because it replaces the implementation before asserting the result.

A strong reviewer should notice that the test can pass even when the production function is incorrect.
